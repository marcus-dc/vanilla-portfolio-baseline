# vanilla-portfolio-baseline

- css/
  - Contains all modularized css. Only imports `index.css` and it's the only one imported in the `index.html` file.
- js/
  - Contains all modularized js. Import all js files in the `index.html`.
